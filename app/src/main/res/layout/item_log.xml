<?xml version="1.0" encoding="utf-8"?>
<!--
  One entry: its headline, then the facts under it. Monospace, as the dialog's text was, because
  these are lines out of a file and columns that line up are how they are compared; selectable,
  because the reader's next move is usually to paste one somewhere.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingTop="10dp"
    android:paddingEnd="16dp"
    android:paddingBottom="10dp">

    <TextView
        android:id="@+id/log_head"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:textIsSelectable="true"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/log_detail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:fontFamily="monospace"
        android:paddingStart="16dp"
        android:textIsSelectable="true"
        android:textSize="12sp" />

</LinearLayout>
