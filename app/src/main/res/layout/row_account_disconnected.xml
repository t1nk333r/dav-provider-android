<?xml version="1.0" encoding="utf-8"?>
<!--
    The card for an Account that was removed with its data kept.

    Its own layout rather than the full card with pieces hidden. Almost nothing on the full card is
    true here — there is no server to sync with, no schedule, no certificate and no Collections —
    and a card assembled by hiding a dozen views would be one edit away from showing something that
    has stopped being true of this state.
-->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <TextView
            android:id="@+id/account_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceTitleMedium" />

        <TextView
            android:id="@+id/account_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceBodyMedium" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Filled: setting it up again is what there is to do with an account in this state. -->
            <Button
                android:id="@+id/account_configure"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/configure_account" />

            <!-- Text weight and the error colour, as on the full card: the only thing here that
                 destroys data. -->
            <Button
                android:id="@+id/account_remove"
                style="?attr/borderlessButtonStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:text="@string/remove_account"
                android:textColor="?attr/colorError" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
