<?xml version="1.0" encoding="utf-8"?>
<!-- supportsUploading="true" is what the framework reads: it now asks for an upload-only sync when
     a provider reports a dirtying write. It is not what a stock editor consults — the Contacts app
     offers this account in "Save contact to" because res/xml/contacts.xml declares an <EditSchema>. -->
<sync-adapter xmlns:android="http://schemas.android.com/apk/res/android"
    android:contentAuthority="com.android.contacts"
    android:accountType="app.davkeep"
    android:userVisible="true"
    android:supportsUploading="true"
    android:allowParallelSyncs="false"
    android:isAlwaysSyncable="true"
    android:settingsActivity="app.davkeep.ui.SettingsActivity" />
